{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des articles</h1>
    <div class="container mb-5 mt-5">
        <h1 class="mb-5">{{ article.articleTitle }}</h1>
        <a class="mb-5">{{ article.articleText }}</a>
    </div>

   {{ form_start(commentForm) }}
        <div class="form-group mt-5">
            <div class="row">
                <div class="col-12 mt-5">
                    {{ form_row(commentForm.comment_article, {'attr': {class : "form-control"}}) }}
                </div>
                <div class="col-12 mt-5">
                    {{ form_row(commentForm.isConfirmed, {'attr': {class : "form-control"}}) }}
                </div>
                <div class="col-12 mt-5">
                    {{ form_row(commentForm.valider, {'attr' : {class : "btn btn-primary"}}) }}
                </div>
            </div>
        </div>
    {{ form_end(commentForm) }}
{#    <h3>Commentaires</h3>#}
{#    <div class="card" style="width: 18rem;">#}
{#        <div class="card-body">#}
{#            {% for comment in article.comments %}#}
{#        <div class="row">#}
{#            <p class="card-text">{{ comment.commentText }}</p>#}
{#            <a href="#" class="card-link">{{ comment.userId }}</a>#}
{#        </div>#}
{#            {% endfor %}#}
{#        </div>#}
{#    </div>#}
{#    <button><a href="{{ path('dashboard_articles_update', {id : app.user.id}) }}">Modifier</a></button>#}

{% endblock %}