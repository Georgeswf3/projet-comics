{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(commentFanArtForm) }}

    <div class="form-group mt-5">
        <div class="row">
            <div class="col-6">
                {{ form_row(commentFanArtForm.comment_fan_art, {'attr': {class: 'form-control'}}) }}
            </div>
        </div>

    </div>

    {{ form_end(commentFanArtForm) }}

{% endblock %}