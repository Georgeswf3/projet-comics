{% extends 'base.html.twig' %}

{% block styleCreateFanArt %}
    <link rel="stylesheet" href="{{ asset ("styles/create-fanart.styles.css")}}">
{% endblock %}

{% block body %}

    <div class="container align-items-center mt-5 mb-5">
        <div class="row justify-content-center">
            <div class="col-lg-7 col-sm-9 contenu">
                <h1 class="mt-5">Ajouter un éditeur : </h1>


                {{ form_start(editorForm) }}
                <div class="form-group mt-4">
                    <div class="row">
                        <div class="col-12 mt-5">
                            {{ form_row(editorForm.editor_brand, {'label':'Editeur','attr': {class : "form-control"}}) }}
                        </div>
                        <div class="col-12 mt-5">
                            {{ form_row(editorForm.ajouter, {'attr' : {class : "btn btn-primary"}}) }}
                        </div>
                    </div>
                </div>
                {{ form_end(editorForm) }}
            </div>
        </div>
    </div>

{% endblock %}